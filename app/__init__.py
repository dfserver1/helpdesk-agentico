"""
Application package for HelpDesk Enterprise Copilot v12.

Import the FastAPI instance from ``app.main`` directly (``from app.main import app``)
to avoid the app.main -> api.routes -> app.rate_limit -> app import cycle.
"""